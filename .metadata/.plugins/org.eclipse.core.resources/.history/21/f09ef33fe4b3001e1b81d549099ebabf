package lambdas;

import java.util.function.Function;

public class Funcao {
	
	public static void main(String[] args) {
		
		Function<Integer, String> parOuImpar = numero -> numero % 2 == 0 ? "par" : "impar";
		
		System.out.println(parOuImpar.apply(33));
	
		Function<String, String> resultado = valor -> "O resultado é " + valor;
		
		System.out.println(resultado.apply(parOuImpar.apply(33)));
		
		Function<String, String> exclamado = valor -> valor + "!!!";
		
		System.out.println(exclamado.apply(parOuImpar.apply(33)));
		
		System.out.println(exclamado.apply(resultado.apply(parOuImpar.apply(33))));
	
	
	}

}
